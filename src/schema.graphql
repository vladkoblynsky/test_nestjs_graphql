type Post {
  id: Int!
  title: String!
  votes: Int
}

type Author {
  id: Int!
  firstName: String
  lastName: String
  posts: [Post]
}

type Query {
  author(id: Int!): Author
  authors(first: Int!): [Author]
  posts(): [Post]
}
